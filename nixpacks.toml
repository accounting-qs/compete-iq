[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate && pip install -r requirements.txt"
]

[start]
cmd = "alembic -c migrations/alembic.ini upgrade head && uvicorn api.main:app --host 0.0.0.0 --port $PORT"
