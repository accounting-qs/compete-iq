[build]
builder = "nixpacks"

[deploy]
startCommand = "alembic -c migrations/alembic.ini upgrade head && uvicorn api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
